<nav class="navbar">
    <% if (!user) { %>
        <!-- Guest -->
        <span>TSC Room Booking</span>
        <div>
            <a href="/" class="<%= currentPage === 'home' ? 'active' : '' %>">Home</a>
            <a href="/auth/login" class="<%= currentPage === 'login' ? 'active' : '' %>">Login</a>
            <a href="/">Contact Us</a>
        </div>

    <% } else if (user.role === 'admin') { %>
        <span>Admin Dashboard</span>
        <div>
            <a href="/admins/dashboard" class="<%= currentPage === 'dashboard' ? 'active' : '' %>">Dashboard</a>
            <a href="/admins/manage-reservations" class="<%= currentPage === 'bookings' ? 'active' : '' %>">Bookings</a>
            <a href="">Users</a>
            <a href="">Rooms</a>
            <a href="">Feedback</a>
            <a href="">Announcement</a>
            <a href="">Logout</a>
        </div>
        
    <% } else if (user.role === 'user') { %>
        <span>User Dashboard</span>
        <div>
            <a href="/users/dashboard" class="<%= currentPage === 'dashboard' ? 'active' : '' %>">Dashboard</a>
            <a href="/reservations/create" class="<%= currentPage === 'bookings' ? 'active' : '' %>">Bookings</a>
            <a href="">Feedback</a>
            <a href="">Logout</a>
        </div>
    <% } %>
</nav>